version: '2.2'

services:

  ### Balancer Haproxy
  balancer:
    image: laradock/haproxy:local.${HAPROXY_TAG}
    build: ${BUILD_ROOT}/haproxy
    ports:
      - "${HAPROXY_HOST_HTTP_PORT}:8085"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    links:
      - proxy
      - proxy2
