version: '2'

services:

  ### Beanstalkd Console Container
  beanstalkd-console:
    image: laradock/beanstalkd-console:local.${BEANSTALKD_CONSOLE_TAG}
    build: ${BUILD_ROOT}/beanstalkd-console
    ports:
      - "2080:2080"
    depends_on:
      - beanstalkd
    networks:
      - backend
