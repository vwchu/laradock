version: '2.2'

services:

  ### HHVM Container
  hhvm:
    image: laradock/hhvm:local.${HHVM_TAG}
    build: ${BUILD_ROOT}/hhvm
    volumes_from:
      - applications
    expose:
      - "9000"
    depends_on:
      - workspace
    networks:
      - frontend
      - backend
