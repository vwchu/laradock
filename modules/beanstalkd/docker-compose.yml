version: '2'

services:

  ### Beanstalkd Container
  beanstalkd:
    build: ${BUILD_ROOT}/beanstalkd
    ports:
      - "${BEANSTALKD_HOST_PORT}:11300"
    privileged: true
    depends_on:
      - php-fpm
    networks:
      - backend
